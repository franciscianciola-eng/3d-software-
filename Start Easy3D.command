#!/bin/bash
# Easy3D Studio — Mac launcher. Double-click me!
# (First time, macOS may warn about an unknown developer:
#  right-click this file → Open → Open. After that, plain double-click works.)
cd "$(dirname "$0")" || exit 1
exec sh start.sh
